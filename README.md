# Udacity PEFT Fine-Tuning Project

This project demonstrates parameter-efficient fine-tuning (PEFT) of a GPT-2 model using LoRA.

The model is fine-tuned on the IMDb dataset for sentiment classification.